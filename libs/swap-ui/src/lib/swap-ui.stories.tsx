import React from 'react';
import { SwapUi, SwapUiProps } from './swap-ui';

export default {
  component: SwapUi,
  title: 'SwapUi',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: SwapUiProps = {};

  return <SwapUi />;
};
